{
  "tool_name": "reference_compare_stage_checkpoint",
  "category": "reference",
  "mode_required": "ANY",
  "selection_required": false,
  "keywords": ["reference", "stage", "checkpoint", "compare", "progress", "vision", "multi-view"],
  "sample_prompts": [
    "capture this stage from deterministic views and compare it to the references",
    "check the current squirrel stage against the front and side reference set",
    "run a staged checkpoint compare for this object and tell me the next corrections"
  ],
  "parameters": {
    "target_object": {"type": "string", "default": null},
    "target_objects": {"type": "array", "default": null},
    "collection_name": {"type": "string", "default": null},
    "checkpoint_label": {"type": "string", "default": null},
    "target_view": {"type": "string", "default": null},
    "goal_override": {"type": "string", "default": null},
    "prompt_hint": {"type": "string", "default": null},
    "preset_profile": {"type": "enum", "options": ["compact", "rich"], "default": "compact"}
  },
  "related_tools": ["reference_images", "reference_compare_current_view", "scene_get_viewport", "router_get_status"],
  "patterns": [],
  "description": "Captures one deterministic multi-view stage checkpoint for a target object using the compact or rich preset profile, then compares that checkpoint set against the active goal plus attached reference images."
}
