{
  "tool_name": "bake_ao",
  "category": "baking",
  "mode_required": "OBJECT",
  "selection_required": false,
  "keywords": ["bake", "ao", "ambient", "occlusion", "shadow"],
  "sample_prompts": ["bake ambient occlusion", "create ao map", "bake shadows"],
  "parameters": {
    "object_name": {"type": "string", "required": true},
    "output_path": {"type": "string", "required": true},
    "resolution": {"type": "int", "default": 1024},
    "samples": {"type": "int", "default": 128},
    "distance": {"type": "float", "default": 1.0},
    "margin": {"type": "int", "default": 16}
  },
  "related_tools": ["bake_normal_map", "bake_combined"],
  "patterns": [],
  "description": "Bakes ambient occlusion map to texture file."
}
