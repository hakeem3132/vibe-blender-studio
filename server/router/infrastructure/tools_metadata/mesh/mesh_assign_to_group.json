{
  "tool_name": "mesh_assign_to_group",
  "category": "mesh",
  "mode_required": "EDIT",
  "selection_required": true,
  "keywords": ["assign", "vertex group", "weight", "add to group"],
  "sample_prompts": [
    "assign to vertex group",
    "add vertices to group",
    "set weight for group",
    "assign selection to head group"
  ],
  "parameters": {
    "object_name": {
      "type": "string",
      "required": true,
      "description": "Name of the mesh object"
    },
    "group_name": {
      "type": "string",
      "required": true,
      "description": "Name of the vertex group"
    },
    "weight": {
      "type": "float",
      "default": 1.0,
      "range": [0.0, 1.0],
      "description": "Weight value for assignment"
    }
  },
  "related_tools": ["mesh_create_vertex_group", "mesh_remove_from_group", "mesh_select"],
  "patterns": [],
  "description": "Assigns selected vertices to a vertex group with weight."
}
